# First install the package
# We are installing version 0 as it supports the dalay option, otherwise, if you installl the latest verison you can't use the delay option
npm i json-server@0

# Next, you need to edit
# todo Next, you need to edit your project's package.json
# todo Add the following script:
# todo "server": "json-server --watch directory/filename.extension --port 8002 --delay 700"
# The port number and delay are optional
