---
tags:
  - SQL
  - sorting
  - sort
---
# SQL Sort order

Using `ORDER BY` a specific column followed by the `ASC` or `DESC` Keywords will return sorted results.

```
SELECT * FROM drivers_license ORDER BY age DESC;
```

