#python #lists #arrays #array-operations #reverting

# Reverting Lists
You can easily reverse a list in python using the bracket notation `some_list[::-1]` or using the range function in a loop `range(len(some_list) - 1, -1, -1)`
