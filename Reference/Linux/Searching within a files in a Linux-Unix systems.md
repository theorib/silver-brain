Tags: #linux #unix #search #cli-commands #find #bash

The grep command stands for find. It takes two arguments, the text you are looking for and the file you want to search
```bash
grep word ./file-name.txt
```